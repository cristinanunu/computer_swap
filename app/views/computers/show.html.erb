<div class="container">

  <div class="title-wrapper mb-4">
    <h3><%= @computer.brand %> <%= @computer.model %></h3>
    <h4>Owner: <%= @computer.user.first_name %> <%= @computer.user.last_name %> &nbsp

    <% if @computer.user.photo.present? %>
      <%= image_tag @computer.user.photo, class: "avatar dropdown-toggle", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
    <% else %>
      <%= image_tag "https://kitt.lewagon.com/placeholder/users/random", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
    <% end %>

  </div>

    <div class="carousel-wrapper">
      <div id="carouselExampleIndicators" class="carousel slide" data-bs-ride="true">
        <div class="carousel-indicators">
          <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
          <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="1" aria-label="Slide 2"></button>
          <button type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide-to="2" aria-label="Slide 3"></button>
        </div>
        <div class="carousel-inner">
          <% @computer.photos.each_with_index do |photo, i| %>
            <div class="carousel-item <%= i == 0 ? "active" : "" %>">
              <%= cl_image_tag photo.key, class: "carousel-img"  %>
            </div>
          <% end %>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>
    </div>


   <div class="show-body-wrapper">

    <div class="details_card w-100 px-4">
      <ul>
      <h3 class="mb-3" style="text-align: left">Specs</h3>
        <li><strong>Year: </strong><%= @computer.year %></li>
        <li><strong>Screen size: </strong><%= @computer.screen_size %> inches </li>
        <li><strong>Processor: </strong><%= @computer.processor %></li>
        <li><strong>Ram: </strong><%= @computer.ram %></li>
        <li><strong>Storage: </strong><%= @computer.storage %></li>
        <li><strong>OS: </strong><%= @computer.os %></li>
        <li><strong>Category: </strong><%= @computer.category %></li>
        <br>
        <p><%= @computer.description %></p>
        <li class="mt-2" style="list-style: none"><h3 style="text-align: right"><%= @computer.price %> €/day</h3></li>
      </ul>
    </div>

  <div class="bookingform w-100">
    <h2><strong>Book this laptop</strong></h2>
    <%= simple_form_for [@computer, @booking] do |f| %>
      <strong><%= f.input :start_date %></strong>
      <br>
      <strong><%= f.input :end_date %></strong>
      <br>
      <div class="btn_form_book">
        <%= f.submit class: "btn btn-flat w-50" %>
      </div>
    <% end %>
  </div>

 </div>


<div class="w-100 mt-3">
  <div class="container-map w-100"
    data-controller="map"
    data-map-markers-value="<%= @marker.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
